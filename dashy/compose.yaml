services:
  dashy:
    image: lissy93/dashy:latest
    ports:
      - "${PORT:-8080}:8080"
    environment:
      NODE_ENV: production
    volumes:
      - ./user-data:/app/user-data
    restart: unless-stopped
